{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
                "buildCommand": "bash setup.sh"
            }
        }
    ],
    "routes": [
        {"src": "/(.*)", "dest": "app.py"}
    ]
}
